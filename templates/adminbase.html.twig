<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Admin - MediaTek Formation</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('admin.formations') }}">Admin MediaTek</a>
                <div class="navbar-nav">
                    <a class="nav-link text-white" href="{{ path('admin.formations') }}">Formations</a>
                    <a class="nav-link text-white" href="{{ path('accueil') }}">Retour au site</a>
                </div>
            </div>
        </nav>
        <div class="container-fluid mt-3">
            {% block body %}{% endblock %}
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>